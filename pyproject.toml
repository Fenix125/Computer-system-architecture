[project]
name = "computer-system-architecture"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.4"
dependencies = [
    "fastapi>=0.129.0",
    "httpx>=0.28.1",
    "uvicorn>=0.41.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
markers = [
    "performance: long-running integration/performance tests against running services",
]
